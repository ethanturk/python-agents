# Minimal dependencies for Agent Vercel serverless deployment
# Only includes what's needed for agent operations (sync, search)

# Core web framework
fastapi
mangum

# LLM and agent framework
pydantic-ai
langchain-openai
litellm

# Database and storage
supabase

# Utilities
python-dotenv
httpx
nest_asyncio

# Note: Heavy dependencies removed for fast builds
# - celery[redis]: Not supported (no background workers on serverless)
# - redis: Not needed
# - docling[vlm]: Not needed (no document ingestion)
# - pypdfium2, pandas, xlrd, openpyxl: Not needed (no file processing)
# - azure-storage-blob: Not needed (no file operations)
# - firebase-admin: Only needed for auth, included if required
# - pytest*: Not needed (test dependencies)
# - uvicorn: Not needed (Mangum handles ASGI)
# - watchdog: Not needed (no file watching)
