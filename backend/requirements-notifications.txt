# Minimal dependencies for Notifications Vercel serverless deployment
# Only includes what's needed for notification operations (poll, notify webhook)

# Core web framework
fastapi
mangum

# Utilities
python-dotenv

# Note: Heavy dependencies removed for fastest builds
# - pydantic-ai, langchain-openai, litellm: Not needed (no LLM operations)
# - supabase: Not needed (in-memory queue only)
# - celery[redis]: Not supported (no background workers on serverless)
# - redis: Not needed
# - azure-storage-blob: Not needed
# - docling[vlm]: Not needed
# - pypdfium2, pandas, xlrd, openpyxl: Not needed
# - firebase-admin: Only needed for auth, included if required
# - pytest*: Not needed (test dependencies)
# - uvicorn: Not needed (Mangum handles ASGI)
# - watchdog: Not needed (no file watching)
