# Minimal dependencies for Vercel serverless deployment
# This is a reference only - each function has its own requirements.txt
# See api/{agent,documents,summaries,notifications}/requirements.txt

# Note: Heavy dependencies removed for fast builds
# - docling[vlm]: Not needed (no document ingestion on serverless)
# - celery[redis]: Not supported (no background workers on serverless)
# - pypdfium2, pandas, xlrd, openpyxl: Not needed (no file processing)
# - pytest*: Not needed (test dependencies)
# - uvicorn: Not needed (Mangum handles ASGI)
# - watchdog: Not needed (no file watching)
# - langchain*: Replaced with pydantic-ai + litellm
# - sse-starlette: Not needed (no SSE endpoints in serverless)
